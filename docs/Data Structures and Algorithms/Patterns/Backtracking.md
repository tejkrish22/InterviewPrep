# Backtracking

## Recognition Cues
- Subsets, Permutations, Combinations, N-Queens, Sudoku Solver.
- Decision tree: **Choice** $\rightarrow$ **Explore** $\rightarrow$ **Un-choose**.

## Common Pitfalls
- Sort array to skip duplicates (`if (i > start && nums[i] == nums[i-1]) continue;`).

## Related Problems
```dataview
LIST FROM "DSA/Problems"
WHERE contains(file.outlinks, this.file.link)
```
