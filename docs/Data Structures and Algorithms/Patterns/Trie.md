# Trie (Prefix Tree)

## Recognition Cues
- Prefix searching, autocomplete systems, dictionary word lookups, Maximum XOR of two numbers.
- Tree where edges represent characters; prefix checks in $O(L)$ time.

## Common Pitfalls
- Unmark nodes properly when backtracking in Word Search II.

## Related Problems
```dataview
LIST FROM "DSA/Problems"
WHERE contains(file.outlinks, this.file.link)
```
