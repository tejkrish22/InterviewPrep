# Greedy

## Recognition Cues
- Locally optimal choice leads to global optimum, interval scheduling, sorting by end times.

## Common Pitfalls
- Sort intervals by end time (not start time) for maximum non-overlapping intervals.

## Related Problems
```dataview
LIST FROM "DSA/Problems"
WHERE contains(file.outlinks, this.file.link)
```
